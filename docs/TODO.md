# TODO — próximos passos

Registro de sessões de `/opsx:explore` que revisaram o `CONTEXT.md` e
mapearam o que falta implementar. Nenhuma decisão é tomada aqui — são
trilhas em aberto para retomar depois.

## Histórico

A trilha registrada em 2026-09-13 — ligar `expandDependencyGraph` e o
parâmetro `scope` de `selectUsesFromSource` à tela de seleção de Root
Unit, que até então só renderizava 1 nível — foi implementada e
arquivada na change `wire-dependency-graph-expansion-ui`
(`openspec/changes/archive/2026-09-13-wire-dependency-graph-expansion-ui/`).
`root-unit-selection` e `dependency-graph-expansion` já refletem o
comportamento atual em `openspec/specs/`.

Nenhuma trilha em aberto no momento.
