## 1. Backup e scaffold

- [x] 1.1 Copiar todo o conteúdo atual do repositório (exceto `.git`) para a pasta de backup `pascal-dependency-viewer-bck/` na raiz do repo e verificar que a cópia é completa (mesma árvore de arquivos do projeto original). (Feito manualmente: `pascal-dependency-viewer-bck/` contém `main.js`, `index.html`, `jest.config.js`, `LICENSE`, `package.json`/`package-lock.json` antigos e `src/components|model|test` completos.)
- [x] 1.2 Mover `.git` para fora da raiz, apagar os demais arquivos/pastas de working tree, rodar `npx create-electron-app@latest --template=vite` na raiz vazia, descartar o `.git` novo gerado pelo scaffold e mover o `.git` original de volta; verificar que a estrutura padrão do template foi criada (Forge config, `vite.main.config.mjs`, `vite.renderer.config.mjs`, `vite.preload.config.mjs`, novo `package.json`) e que `git log` ainda mostra o histórico original. (Feito: `forge.config.js`, `vite.*.config.mjs`, `src/{main,preload,renderer}.js` presentes na raiz; `git log` mostra os commits originais mais um commit `temp`.)
- [x] 1.3 Comparar a versão de `electron`/Forge trazida pelo scaffold com a atual (`^44.3.0`) e decidir se atualiza ou fixa a versão, registrando a decisão no `package.json` final; verificar que `npm start` abre a janela padrão do template antes de portar qualquer código. (Feito: scaffold trouxe `electron@44.3.0`, igual à versão já fixada no projeto - nenhuma decisão de upgrade/downgrade necessária.)
- [x] 1.4 Trazer `src/model/**` e `src/test/**` de volta da pasta de backup `pascal-dependency-viewer-bck/` para o novo projeto, sem alteração de caminho ou conteúdo, e rodar `npm test` para confirmar que a suíte Jest continua verde. (Também trazido `jest.config.js` e adicionados `jest` como devDependency + script `test` no `package.json`, necessários para `npm test` funcionar; suíte passando - na época 44/44 porque `pascal-dependency-viewer-bck/src/test/**` ainda continha os mesmos `*.test.js` e o Jest rodava ambas as cópias; depois que a tarefa 6.1 removeu essa pasta, o número correto é 22/22, 5 suites - ver nota em 5.1. Aviso inofensivo de colisão de nome do Haste por causa do `package.json` duplicado em `pascal-dependency-viewer-bck/`, que também sumiu com a remoção da pasta.)
- [x] 1.5 Adicionar `pascal-dependency-viewer-bck/` ao `.gitignore` (ou confirmar que já está coberto) e verificar com `git status` que ela aparece como ignorada, para evitar que seja commitada por engano antes de ser removida na tarefa 6.1. (Feito: entrada `/pascal-dependency-viewer-bck/` adicionada ao `.gitignore`; a pasta estava staged de um `git add -A` anterior - rodado `git reset -- pascal-dependency-viewer-bck/` para tirá-la do índice; `git check-ignore -v` e `git status --ignored` confirmam que agora é ignorada.)

## 2. Camada main (processo principal)

- [x] 2.1 Portar `src/components/Main/Window/index.js` (da pasta de backup `pascal-dependency-viewer-bck/`) para o novo entry point `main` do template, criando o `BrowserWindow` com `contextIsolation: true` / `nodeIntegration: false` e `preload` apontando para o preload do template; verificar que a janela ainda abre. (Implementado em `src/main.js`; confirmado que o app inicia sem erros via `electron-forge start` sob Xvfb - ver nota em 5.2/5.3 para a validação visual completa.)
- [x] 2.2 Implementar handler IPC `ipcMain.handle('openFile', ...)` que reaproveita `getUnitName`, `selectUsesFromSource`, `mountDependenceGraphStructure` e retorna `{ nodes, edges }`; verificar com log/teste manual que o handler retorna o shape esperado para um `.pas` de teste (`src/test/souceUsesInInterface.pas`). (Verificado via script Node isolado: `performOpenFile`/lógica equivalente retorna `{ nodes, edges }` com o shape correto para o fixture de teste.)
- [x] 2.3 Implementar handler IPC `ipcMain.handle('openProject', ...)` que reaproveita `parseDprSource`, retornando `{ projectUnits, projectDir }`; verificar com o `.dpr` de teste (`src/test/sourceDprWithProjectUnits.dpr`). (Verificado via script Node isolado: retorna `projectUnits` com o shape `{unitName, path}` esperado.)
- [x] 2.4 Implementar handler IPC `ipcMain.handle('expandFromRootUnit', ...)` que recebe `{ projectDir, projectUnit, projectUnits, scope }`, lê os arquivos `.pas` necessários do disco (movendo essa leitura do renderer para o main) e roda `expandDependencyGraph`, retornando `{ nodes, edges }`. (Wrapper fino sobre `expandDependencyGraph`, já coberto pelos 44 testes Jest existentes; lógica de leitura de arquivo idêntica à do `index.html` antigo.)
- [x] 2.5 Recriar o menu (`File > Open`, `File > Open Project`) no novo entry point `main`, disparando os handlers acima e enviando o resultado para o renderer (via retorno do `invoke` ou `webContents.send`), sem depender de `electron-store`/`reloadMainWindow()`. (Implementado: clique do menu chama a função `perform*` e envia o resultado via `mainWindow.webContents.send('app:graph-loaded'|'app:project-loaded', ...)`; nenhum uso de `electron-store`/`reloadMainWindow`.)
- [x] 2.6 Remover (ou condicionar a uma flag de dev) o `webContents.openDevTools()` incondicional herdado de `Window/index.js`. (Condicionado a `!app.isPackaged`.)

## 3. Preload

- [x] 3.1 Criar/editar `preload.js` do template expondo via `contextBridge.exposeInMainWorld` apenas os três métodos `openFile`, `openProject`, `expandFromRootUnit` (cada um invocando o canal IPC correspondente); verificar que não há nenhuma superfície genérica tipo `require` exposta ao renderer. (Implementado em `src/preload.js` sob o nome `window.pascalDependencyViewer`, com os 3 métodos de invoke + 2 listeners `onGraphLoaded`/`onProjectLoaded` para receber o push do menu; nenhum `require` exposto.)

## 4. Renderer

- [x] 4.1 Portar o HTML de `index.html` (da pasta de backup `pascal-dependency-viewer-bck/`: divs `rootUnitSelection`, `mynetwork`, controles de filtro/escopo) para o template do entry point `renderer`, mantendo os mesmos ids de elemento. (Feito; CSS padrão do template (`src/index.css`, `max-width: 38rem`) removido/não importado para preservar o layout original do grafo em tela cheia.)
- [x] 4.2 Reescrever o `<script>` inline como módulo JS do renderer (`import { DataSet } from 'vis-data'`, `import { Network } from 'vis-network'`), preservando `renderGraph` com o mesmo `options`/`groups`/`physics` de hoje. (Feito em `src/renderer.js`.)
- [x] 4.3 Reescrever `selectRootUnit`/`renderRootUnitList`/`renderRootUnitSelection` para chamar `window.<api exposta pelo preload>.expandFromRootUnit(...)` em vez de `fs.readFileSync`/`expandDependencyGraph` direto no renderer. (Feito - `selectRootUnit` agora é `async` e chama `window.pascalDependencyViewer.expandFromRootUnit(...)`.)
- [x] 4.4 Ligar o clique de menu "Open"/"Open Project" ao fluxo do renderer: chamar `openFile`/`openProject` da API exposta e desenhar o resultado (`renderGraph` ou `renderRootUnitSelection`) sem depender de reload de página inteira. (Feito via listeners `onGraphLoaded`/`onProjectLoaded` registrados no carregamento do renderer - o menu (main) dispara a ação e o push IPC atualiza a tela sem reload.)
- [x] 4.5 Buscar por `require(` remanescente em qualquer arquivo do novo renderer e confirmar que não há nenhum (toda a superfície Node/Electron passa pelo preload). (Confirmado via grep: nenhuma ocorrência em `index.html`/`src/renderer.js`/`src/preload.js`.)

## 5. Validação

- [x] 5.1 Rodar `npm test` e confirmar que toda a suíte Jest (`src/model/**`, `src/test/**`) continua passando sem nenhuma alteração de arquivo dessas pastas. (Passando neste ponto ainda com a duplicação da pasta de backup contada - ver nota final em 6.1: após a tarefa 6.1 remover `pascal-dependency-viewer-bck/`, a contagem real e correta é 22/22 testes, 5 suites, igual ao projeto original antes da migração.)
- [x] 5.2 Usar a skill `run` para abrir o app, testar File > Open com um `.pas` avulso e confirmar visualmente que o grafo de 1 nível é desenhado igual ao comportamento anterior. (Sem skill de projeto para rodar o app; seguido o padrão Electron da skill `run` - driver Playwright `_electron` sob Xvfb, empacotando o app via `electron-forge package` e mockando `dialog.showOpenDialog` no processo main. Testado com `src/test/souceUsesInInterface.pas`: grafo de 1 nível renderizado corretamente - `untExercicio04` no centro com edges para as 10 dependências do `uses` da interface. Screenshot confere visualmente.)
- [x] 5.3 Usar a skill `run` para testar File > Open Project, selecionar uma Root Unit, alternar entre os dois Uses Clause Scopes (`interface` e `interface+implementation`) e confirmar que o grafo expandido é equivalente ao gerado pela versão antiga do app. (Testado com um mini-projeto `.dpr` criado para o teste (`TestProject.dpr` com `UnitA`/`UnitB`): tela de seleção de Root Unit renderiza a lista corretamente, filtro por texto funciona, clique expande o grafo via `expandFromRootUnit`. Grafo resultante bate com o esperado - `UnitA` (projectUnit) -> `UnitB` (projectUnit) -> `System.SysUtils` (externalUnit), `UnitA` -> `Winapi.Windows` (externalUnit), cores/grupos corretos. Testado nos dois escopos (`interface` e `interface+implementation`); nenhum erro de console JS capturado em nenhum dos cenários. Nota: para o Playwright conseguir anexar ao binário empacotado, o fuse `EnableNodeCliInspectArguments` do `forge.config.js` precisou ser temporariamente ligado durante o teste e foi revertido para `false` logo em seguida - confirmado via `git diff` que o arquivo final no working tree bate com o original.)

## 6. Limpeza e documentação

- [x] 6.1 Remover a pasta de backup `pascal-dependency-viewer-bck/` de referência (ou movê-la para fora do repositório) e qualquer dependência órfã do `package.json`, só depois que as tasks da seção 5 passarem. (Pasta removida; entrada correspondente retirada do `.gitignore`. Nenhuma dependência órfã encontrada no `package.json` - `electron-store` nunca chegou a ser adicionado ao novo `package.json`, então não havia nada para remover. Confirmado com `npm test` após a remoção: 22/22 testes, 5 suites - o número correto, sem a duplicação que a pasta de backup causava por conter os mesmos `*.test.js`.)
- [x] 6.2 Atualizar `CLAUDE.md` para descrever a nova divisão de processos (main/preload/renderer via Vite), os novos comandos (`npm start`, `npm run package`/`npm run make` se configurados) e o novo mecanismo de comunicação via IPC, substituindo as referências a `electron-store` como canal compartilhado. (Reescrito: Commands em npm, Architecture descrevendo main/preload/renderer + fluxo IPC completo, Notes atualizadas incluindo a peculiaridade do `commonjsOptions.include` no `vite.main.config.mjs`.)
