## 1. Catálogo e lógica pura de Commands (domain)

- [x] 1.1 Criar `src/domain/commands/index.js` com `COMMANDS` (`openProject`, `openRecent`, `selectUnit` com `requiresProject: true`, nesta ordem, cada um com `id`, `label` e o atalho quando existir), `availableCommands(commands, { hasProject })`, `filterCommands(commands, text)` (substring do rótulo, sem diferenciar maiúsculas nem acentos, preservando a ordem do catálogo) e `shortcutHint(command, isMac)` (`⌘O`/`Ctrl+O`, `⌘K R`/`Ctrl+K R`, `undefined` para `selectUnit`); cobrir cada cenário da spec `command-palette` (catálogo e ordem, `selectUnit` omitido sem Project, filtro por parte do rótulo, por maiúsculas e por acentos, sem resultado, dicas por plataforma) em `src/domain/test/commands.test.js` e verificar com `npx jest src/domain/test/commands.test.js`

## 2. Execução de Commands e IPC (main)

- [x] 2.1 Criar `src/main/commands.js` com `runCommand(id, { webContents, performOpenProject })`: `openProject` envia `app:project-loaded` só se `performOpenProject` devolver Project (cancelar não envia nada), `selectUnit` envia `app:show-root-unit-selection`, `openRecent` envia `app:show-open-recent`, `id` desconhecido é ignorado; testar em `src/main/test/commands.test.js` (cancelar o diálogo não envia `app:project-loaded`, cada `id` envia o evento certo, `id` desconhecido não faz nada) e verificar com `npx jest src/main/test/commands.test.js`
- [x] 2.2 Criar `src/main/ipc/commands.js` com `registerCommandsHandler(ipcMain, { performOpenProject })` que trata `commands:run` chamando `runCommand(id, { webContents: event.sender, performOpenProject })`, ligá-lo em `src/main/index.js` ao lado dos handlers existentes e verificar em `src/main/test/commands.test.js` que o handler `commands:run` é registrado e dispara `runCommand` com o `event.sender`

## 3. Menu (main)

- [x] 3.1 Em `src/main/menu.js`, fazer os itens `Abrir projeto (.dpr)`, `Abrir recente` (menu `Arquivo`) e `Selecionar Unit` (menu `Edição`) chamarem `runCommand` pelo `id` do catálogo, sem mudar rótulos, aceleradores nem a dica `⌘K R` no rótulo; verificar em `src/main/test/menu.test.js` que os testes existentes continuam passando e que os rótulos desses três itens batem com os do catálogo (o de `Abrir recente` com a dica anexada)
- [x] 3.2 Em `src/main/menu.js`, adicionar o menu `Ferramentas` entre `Edição` e `{ role: "viewMenu" }`, com um único item `Paleta de Comandos`, `accelerator: "CmdOrCtrl+P"` e `click` que envia `app:show-command-palette`; atualizar `src/main/test/menu.test.js` (ordem `Arquivo`, `Edição`, `Ferramentas`, `viewMenu`; `Ferramentas` com exatamente um item, o acelerador e o envio do evento) e verificar com `npx jest src/main/test/menu.test.js`

## 4. Preload e renderer

- [x] 4.1 Em `src/preload/api.js`, expor `runCommand(id)` (`ipcRenderer.invoke('commands:run', id)`), `onShowCommandPalette(cb)` (com função de remoção do listener, como os demais) e `platform` (`process.platform`); verificar que segue o padrão dos listeners existentes e que `npm start` sobe sem erro no console
- [x] 4.2 Em `index.html`, renomear o overlay `#openRecent*` para `#commandPalette*` (mesmo estilo), tornar o prefixo `Abrir recente` ocultável, adicionar a dica de atalho alinhada à direita em cada linha da lista e mensagens de lista vazia por modo (`Nenhum comando encontrado` / `Nenhum projeto recente`); verificar visualmente na tarefa 5.1
- [x] 4.3 Criar `src/renderer/components/commandPalette.js` a partir de `openRecentDialog.js` (que é removido), com `showCommandPalette()` e `showOpenRecent()`, os modos `commands` e `recents`, `recentsEntry` (`palette`/`direct`) e as regras: modo `commands` sem prefixo, placeholder `Digite um comando`, lista de `filterCommands(availableCommands(COMMANDS, { hasProject }), texto)` com `shortcutHint` à direita, `↑`/`↓`/`Enter`/`Esc`/clique/clique fora, `Enter` sem itens sem efeito; `openRecent` troca para `recents` (`palette`), os demais fecham a paleta e chamam `api.runCommand(id)`; no modo `recents`, o comportamento atual do diálogo e `Esc` voltando a `commands` se `recentsEntry === 'palette'`, senão fechando; `showCommandPalette()` com a paleta já aberta volta a `commands`, limpa o input e foca; verificar manualmente na tarefa 5.1
- [x] 4.4 Em `src/renderer/index.js`, trocar `showOpenRecentDialog` por `showOpenRecent`, chamar `showCommandPalette()` em `api.onShowCommandPalette`, informar `hasProject: () => lastProject !== null` ao componente e fechar a paleta ao receber `app:project-loaded`; verificar manualmente na tarefa 5.1

## 5. Verificação e documentação

- [x] 5.1 Rodar `npm start` e verificar os cenários das specs `command-palette`, `application-menu` e `recent-projects`: menu `Ferramentas > Paleta de Comandos` com `Cmd/Ctrl+P` exibido; atalho e clique abrem a paleta no modo lista de Commands, com e sem Project aberto (`Selecionar Unit` só aparece com Project); dicas de atalho; filtro por parte do rótulo, maiúsculas e acentos, e `Nenhum comando encontrado`; `↑`/`↓`/`Enter`/`Esc`/clique/clique fora; `Abrir projeto (.dpr)` pela paleta (paleta fecha antes do diálogo nativo, cancelar não reabre a paleta, escolher um `.dpr` leva à seleção de Root Unit); `Selecionar Unit` pela paleta; `Abrir recente` pela paleta troca para o modo recentes e `Esc` volta à lista de Commands; `Cmd/Ctrl+K R` e `Arquivo > Abrir recente` abrem direto no modo recentes e `Esc` fecha, inclusive com a paleta já aberta na lista de Commands; `Cmd/Ctrl+P` no modo recentes volta à lista de Commands; abrir um Recent Project e o erro de arquivo ausente continuam funcionando; `Cmd/Ctrl+O` com a paleta aberta fecha a paleta ao carregar o Project
- [x] 5.2 Rodar `npm test` e verificar que a suíte inteira passa
- [x] 5.3 Atualizar `CLAUDE.md` (menu `Ferramentas`, `src/domain/commands/`, `src/main/commands.js` e `ipc/commands.js`, `commands:run` e `app:show-command-palette`, `commandPalette.js` no lugar de `openRecentDialog.js`, `platform` no preload e o novo trecho do fluxo de dados) e verificar que a descrição bate com o código implementado
